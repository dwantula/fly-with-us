import React from 'react';
// import { useSelector } from 'react-redux';

import './styles.scss';

function MyBooking() {
  // const countries = useSelector((state) => state.countries);
  return (
    <div>
      {/* {countries.map((elem) => (
        <ul>
          <li>{console.log(elem)}</li>
        </ul>
      ))} */}
      aa
    </div>
  );
}

export default MyBooking;
